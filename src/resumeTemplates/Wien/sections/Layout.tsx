import React from "react";

import { ResumeTemplateProps } from "../../../components/misc";

export const Layout: ResumeTemplateProps["Layout"]  = ({ 
    Header, 
    Orderable, 
    ContactDetails, 
    ProfessionalExperience 
}) => {

    return (
        <>
        </>
    );
}