import React from "react";

import { ResumeTemplateProps } from "../../../components/misc";

export const Description: ResumeTemplateProps["Description"] = ({ 
    sectionName, 
    description 
}) => {
    return (
        <>
        </>
    );
}