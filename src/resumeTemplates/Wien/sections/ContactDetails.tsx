import React from "react";

import { ResumeTemplateProps } from "../../../components/misc";

export const ContactDetails: ResumeTemplateProps["ContactDetails"] = ({
    email,
    mobilenumber,
    country,
    city,
    address,
    zipCode,
    driverLicense,
    nationality,
    birthDate,
    birthPlace
}) => {
    return (
        <>
        </>
    );
}