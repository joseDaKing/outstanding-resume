import React from "react";

import { ResumeTemplateProps } from "../../../components/misc";

export const Reference: ResumeTemplateProps["Reference"] = ({ 
    sectionName, 
    Items 
}) => {
    return (
        <>
        </>
    );
}