import React from "react";

import { ResumeTemplateProps } from "../../../components/misc";

export const Skill: ResumeTemplateProps["Skill"] = ({ 
    sectionName, 
    Items 
}) => {
    return (
        <>
        </>
    );
}