import React from "react";

import { ResumeTemplateProps } from "../../../components/misc";

export const Link: ResumeTemplateProps["Link"] = ({ 
    sectionName, 
    Items 
}) => {
    return (
        <>
        </>
    );
}