import React from "react";

import { ResumeTemplateProps } from "../../../components/misc";

export const Course: ResumeTemplateProps["Course"] = ({ 
    sectionName, 
    Items 
}) => {
    return (
        <>
        </>
    );
}