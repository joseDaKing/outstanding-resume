import React from "react";

import { IResumeTemplateProps } from "../../../components/misc";

export const Language: IResumeTemplateProps["Skill"] = ({ 
    sectionName, 
    Items 
}) => {
    return (
        <>
        </>
    );
}